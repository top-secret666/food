server:
  port: ${PORT:8081}

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
  datasource:
    url: jdbc:h2:mem:restaurant_db;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;INIT=CREATE SCHEMA IF NOT EXISTS restaurant_schema\;SET SCHEMA restaurant_schema
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        default_schema: restaurant_schema
  liquibase:
    enabled: false
  kafka:
    listener:
      auto-startup: false
    admin:
      fail-fast: false

security:
  require-email-verified: false

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001,https://aroma-food.vercel.app,https://reactfistapp.vercel.app,https://top-secret666.github.io}

app:
  jwt:
    secret: ${APP_JWT_SECRET:aroma-local-dev-secret-key-32bytes!!}
  kafka:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
